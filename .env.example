# ShareWord Deployment Configuration

# The port the server will listen on
PORT=8080

# The path to the SQLite database file
DB_PATH=shareword.db

# Environment mode: 'development' or 'production'
# Setting this to 'production' enables Secure cookies (HTTPS only)
ENV=development

# Optional: Set to 'true' if using a hot-reload development tool
HOT_RELOAD=false
