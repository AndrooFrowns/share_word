services:
  app:
    build: .
    container_name: shareword
    ports:
      - "80:8080"
    volumes:
      - ./data:/data
    environment:
      - PORT=8080
      - DB_PATH=/data/shareword.db
      - ENV=production
    restart: always
